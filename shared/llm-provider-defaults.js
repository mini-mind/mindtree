const PRIMARY_PROVIDER_DEFAULTS = {
  baseUrl: "https://api.siliconflow.cn/v1",
  model: "deepseek-ai/DeepSeek-V3.2",
};

const DEFAULT_AGENT_OVERRIDES = {
  assistant: {},
};

module.exports = {
  PRIMARY_PROVIDER_DEFAULTS,
  DEFAULT_AGENT_OVERRIDES,
};
